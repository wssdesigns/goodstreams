import React from 'react'

export default () => {
  return (
    <div style={{ marginBottom: '60px' }}>
    <Button
      onClick={this.onDeleteClick.bind(this)}
      outline color="light"
    >
      Delete My Account
    </Button>
  </div>
  )
}
